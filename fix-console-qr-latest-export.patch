diff --git a/service/Dockerfile b/service/Dockerfile
index 6f3f5e0..210c54e 100644
--- a/service/Dockerfile
+++ b/service/Dockerfile
@@ -3,11 +3,11 @@ FROM python:3.12-slim
 WORKDIR /app
 
 COPY pyproject.toml ./
-COPY src ./src
+COPY console_text_qr ./console_text_qr
 COPY tests ./tests
 COPY examples ./examples
 
-RUN pip install --no-cache-dir -e ".[dev]"
+RUN python -m pip install --no-cache-dir -e ".[dev]"
 
 ENTRYPOINT ["console-qr"]
 CMD ["examples/hello.txt"]
diff --git a/service/pyproject.toml b/service/pyproject.toml
index 0d88107..56981e1 100644
--- a/service/pyproject.toml
+++ b/service/pyproject.toml
@@ -6,9 +6,8 @@ build-backend = "setuptools.build_meta"
 name = "console-qr"
 version = "0.1.0"
 description = "Print a text file as a QR code in the console."
-readme = "README.md"
 requires-python = ">=3.10"
-license = { text = "MIT" }
+license = { text = "Apache-2.0" }
 authors = [
   { name = "Masaaki Ikeda" }
 ]
@@ -22,11 +21,12 @@ dev = [
 ]
 
 [project.scripts]
-console-qr = "console-qr.cli:main"
+console-qr = "console_text_qr.cli:main"
 
 [tool.setuptools.packages.find]
-where = ["src"]
+where = ["."]
+include = ["console_text_qr*"]
 
 [tool.pytest.ini_options]
 testpaths = ["tests"]
-pythonpath = ["src"]
+pythonpath = ["."]
diff --git a/service/tests/test_cli.py b/service/tests/test_cli.py
index b81cfcc..ca60665 100644
--- a/service/tests/test_cli.py
+++ b/service/tests/test_cli.py
@@ -1,6 +1,6 @@
 from __future__ import annotations
 
-from service.console-qr.cli import main, read_text_file, render_matrix, render_text_as_qr
+from console_text_qr.cli import main, read_text_file, render_matrix, render_text_as_qr
 
 
 def test_read_text_file_preserves_trailing_newline(tmp_path):
